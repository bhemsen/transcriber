[package]
name = "transcriber-audio"
description = "PCM frame and format types, the fixed-size per-stream ring buffer, and read-side downmix/resampling to 16 kHz mono. No I/O, no HTTP client."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rubato = "0.15"
thiserror = "2"
zeroize = "1"

[lints]
workspace = true
